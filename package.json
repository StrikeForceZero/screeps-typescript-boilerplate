{
  "name": "screeps-typescript-boilerplate",
  "version": "0.1.0",
  "description": "WIP boilerplate for screeps with typescript support",
  "scripts": {
    "build:clean": "rimraf dist && rimraf .awcache",
    "prebuild": "npm run build:clean",
    "build:watch": "npm run build -- --watch",
    "build": "webpack --config webpack.config.js",
    "start": "npm run build:watch",
    "deploy": "node deploy.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brett Striker <StrikeForceZero@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "awesome-typescript-loader": "^2.2.1",
    "bluebird": "^3.4.1",
    "chalk": "^1.1.3",
    "expose-loader": "^0.7.1",
    "progress-bar-webpack-plugin": "^1.9.0",
    "rimraf": "^2.5.4",
    "tslint": "^3.14.0",
    "tslint-loader": "^2.1.5",
    "typescript": "^2.0.0",
    "webpack": "^1.13.1"
  }
}
